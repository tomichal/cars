%main
  %ul
    - for data in DATA.sort_by{ |item| item['name'] }
      %li
        = image_tag '/images/phones.jpg'
        = link_to data['name'], :class => "link_#{data['id']}"
      
        %div{:style => 'display:none', :class => "info"}